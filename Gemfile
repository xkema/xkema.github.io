# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "jekyll"
gem "jekyll-sitemap" # This has no effect on GitHub Pages. GitHub pages use "plugins" option inside "_config.yml". This is for local environment.
gem "jekyll-feed"

# windows environment requirements (comment out on ruby-ready environments)
gem "wdm"
gem "tzinfo"
gem "tzinfo-data"
gem "webrick"
