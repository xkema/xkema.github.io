# frozen_string_literal: true

source "https://rubygems.org"

git_source(:github) {|repo_name| "https://github.com/#{repo_name}" }

gem "jekyll"
gem "jekyll-sitemap" # This has no effect on GitHub Pages. GitHub pages use "plugins" option inside "_config.yml". This is for local environment.